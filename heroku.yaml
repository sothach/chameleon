build:
  docker:
    web: target/docker/stage/opt/docker/bin/chameleon -Dhttp.port=${PORT} -Dplay.evolutions.db.default.autoApply=true -Ddb.default.driver=org.postgresql.Driver -Ddb.default.url=${DATABASE_URL}
setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE